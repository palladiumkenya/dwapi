namespace Dwapi.SharedKernel.Enum
{
    public enum ExtractStatus
    {
        Idle,
        Clearing, Cleared,
        Finding,Found,
        Loading,Loaded,
        Validating,Validated,
        Sending,Sent,
        Excluded,Rejected
    }
}
